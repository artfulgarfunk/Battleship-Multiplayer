import React from 'react';
import ReactDOM from 'react-dom';
import 'whatwg-fetch';

const contentNode = document.getElementById('contents');
const component = <h1> BATTLESHIP </h1>;
ReactDOM.render(component, contentNode);
